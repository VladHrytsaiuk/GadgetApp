using Avalonia.Controls;
using GadgetApp.ViewModels; // Підключаємо нашу нову ViewModel

namespace GadgetApp.Views
{
    public partial class MainWindow : Window
    {
        public MainWindow()
        {
            InitializeComponent();

            // Створюємо екземпляр ViewModel і встановлюємо його як контекст даних для вікна.
            // Тепер вікно "знає", звідки брати дані та команди.
            DataContext = new MainWindowViewModel();
        }
    }
}